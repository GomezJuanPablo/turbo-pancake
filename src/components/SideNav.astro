---
interface Props {
  activeNav?: string;
}
const { activeNav = '' } = Astro.props;

const groups = [
  {
    label: 'Practice',
    items: [
      { id: 'csa',      label: 'CSA',       href: '/csa/' },
      { id: 'cad',      label: 'CAD',       href: '/cad/' },
      { id: 'cis-itsm', label: 'CIS-ITSM',  href: '/cis-itsm/' },
      { id: 'cis-df',   label: 'CIS-DF',    href: '/cis-df/' },
      { id: 'cis-sm',  label: 'CIS-SM',  href: '/cis-sm/' },
      { id: 'cis-rc',  label: 'CIS-RC',  href: '/cis-rc/',  disabled: true },
      { id: 'cis-ham', label: 'CIS-HAM', href: '/cis-ham/', disabled: true },
      { id: 'cis-discovery', label: 'CIS-Discovery', href: '/cis-discovery/', disabled: true },
      { id: 'cis-em', label: 'CIS-EM', href: '/cis-em/', disabled: true },
      { id: 'cis-hr', label: 'CIS-HR', href: '/cis-hr/', disabled: true },
      { id: 'cis-pa', label: 'CIS-PA', href: '/cis-pa/', disabled: true },
    ],
  },
  {
    label: 'Library',
    items: [
      { id: 'roadmap',     label: 'Roadmap',     href: '/roadmap/' },
      { id: 'about',       label: 'About',       href: '/about/' },
    ],
  },
];
---
<aside class="sidenav" aria-label="Primary navigation">
  {groups.map((group) => (
    <div class="sidenav__group">
      <div class="sidenav__group-label">{group.label}</div>
      {group.items.map((item) => (
        <a
          href={item.disabled ? '#' : item.href}
          class={`sidenav__item ${activeNav === item.id ? 'is-active' : ''}`}
          aria-disabled={item.disabled ? 'true' : 'false'}
          target={item.external ? '_blank' : undefined}
          rel={item.external ? 'noopener noreferrer' : undefined}
          style={item.disabled ? 'opacity:0.45;pointer-events:none;' : ''}
        >
          {item.label}
          {item.disabled && <span class="sidenav__badge">soon</span>}
          {item.external && <span class="sidenav__ext" aria-hidden="true">↗</span>}
        </a>
      ))}
    </div>
  ))}
</aside>
